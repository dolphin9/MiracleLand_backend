info:
  name: me
  type: http
  seq: 8

http:
  method: GET
  url: http://localhost/miracleland_backend/wordpress/wp-json/wp/v2/users/me
  auth:
    type: basic
    username: "{{adminUser}}"
    password: "{{adminAppPassword}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
