info:
  name: list_categories
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/creation-categories"
  auth: inherit

runtime:
  assertions:
    - expression: res.body
      operator: isDefined

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
