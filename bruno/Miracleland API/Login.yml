info:
  name: Login
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/"
  body:
    type: json
    data: ""
  auth:
    type: basic
    username: "{{adminUser}}"
    password: "{{adminAppPassword}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
