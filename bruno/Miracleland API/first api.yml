info:
  name: first api
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost/miracleland_backend/wordpress/wp-json/miracleland/v1/oc
  auth: inherit

runtime:
  assertions:
    - expression: res.body
      operator: isDefined

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
